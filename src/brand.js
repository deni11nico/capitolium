/**
 * The property's name, in one place.
 *
 * The wordmark sets the two parts on separate lines, the address line above
 * the descriptor, so they are kept separate here rather than as one string.
 * Anything user-facing should read from these rather than hardcoding the name.
 */
export const BRAND_NAME = 'Maniu 65'
export const BRAND_DESCRIPTOR = 'Central'
export const BRAND_FULL = `${BRAND_NAME} ${BRAND_DESCRIPTOR}`
